
# 🦅 StorkCrypto Design System v12.1
> **Concept:** "Neural Terminal" — High-Contrast Cyberpunk Fintech.
> **Philosophy:** Максимальна чіткість, глибина чорного (Deep Void), неонові акценти та тактильність інтерфейсу.

---

## 1. 🎨 Колірна Палітра (Color DNA)

Ми використовуємо палітру "Deep Void" для досягнення максимального контрасту на OLED-екранах. Стандартний чорний (`#000000`) використовується рідко, перевага надається глибоким відтінкам синього/індиго.

### Основні кольори
| Назва | Hex | Tailwind Class | Використання |
| :--- | :--- | :--- | :--- |
| **Deep Void** | `#020617` | `bg-brand-bg` | Глобальний фон. Створює ефект безмежної глибини. |
| **Cyber Cyan** | `#00F0FF` | `text-brand-cyan` | Основні дії, активні стани, сканери. Електричний, різкий. |
| **Electric Purple** | `#BD00FF` | `text-brand-purple` | AI, Преміум функції, "Whale" статус. |
| **Neon Mint** | `#00FF9D` | `text-brand-green` | Профіт, успіх, позитивні тренди. |
| **Cyber Red** | `#FF0055` | `text-brand-danger` | Помилки, збитки, "Sniper Mode". |

### Структурні кольори (UI Shell)
*   **Glass Surface:** `rgba(15, 23, 42, 0.85)` (`bg-brand-card`) — Основний колір карток.
*   **Borders:** `rgba(255, 255, 255, 0.1)` — Тонкі лінії для розділення контенту.
*   **Overlay:** `bg-black/90` — Для модальних вікон та бекдропів.

---

## 2. 🔠 Типографіка (Font Triad)

Використовується три шрифти для чіткого розмежування типів інформації.

### 1. Orbitron (Headers & HUD)
Футуристичний, широкий шрифт без засічок.
*   **Використання:** Заголовки, логотип, кнопки дій (CTA), статуси "SCANNING...".
*   **Стиль:** Завжди `uppercase`, `tracking-widest`, `font-black`.

### 2. Space Mono (Data & Numbers)
Моноширинний шрифт для табличних даних.
*   **Використання:** Ціни, відсотки, хеші транзакцій, таймери.
*   **Перевага:** Цифри не "скачуть" при оновленні в реальному часі.

### 3. Inter (Body & UI)
Нейтральний гротеск для читабельності.
*   **Використання:** Описи, новини, налаштування, довгі тексти.

---

## 3. 🧩 Архітектура Компонентів (UI Kit)

### A. Картки (Futuristic Card)
Картки не мають тіней у класичному розумінні. Глибина створюється через шари прозорості та бордери.

**Стиль:**
*   **Radius:** `rounded-[2rem]` або `rounded-3xl` (Hyper-rounded).
*   **Background:** `bg-[#0a0f1e]/80` + `backdrop-blur-xl`.
*   **Border:** `border border-white/5` (ледь помітний) -> `hover:border-brand-cyan/30`.
*   **Inner Shadow:** Використовується `shadow-inner` для створення ефекту "вдавленого" екрану.

```tsx
<div className="relative rounded-[2rem] bg-brand-card/60 backdrop-blur-xl border border-white/10 shadow-2xl overflow-hidden group">
  {/* Glow Effect on Hover */}
  <div className="absolute inset-0 bg-brand-cyan/5 opacity-0 group-hover:opacity-100 transition-opacity"></div>
  {/* Content */}
</div>
```

### B. Навігація (Glass Capsule)
Нижня навігація не прилипає до низу екрану, а "плаває" (`floating`) над контентом.

**Специфікація:**
*   **Position:** `fixed bottom-6`.
*   **Shape:** `rounded-3xl` (Capsule).
*   **Material:** High-blur glass (`backdrop-blur-2xl`).
*   **Active State:** "Light Pillar" effect — вертикальний градієнт, що виходить з активної іконки вгору.

### C. Іконографія
*   Всі іконки — **SVG (Vector)**.
*   `stroke-width="2"` для чіткості на Retina дисплеях.
*   Іконки часто мають "свічіння" через `drop-shadow` у CSS.

---

## 4. 📱 Мобільна Адаптація (Mobile-First DNA)

Проект спроектовано як **Native App** у браузері. Ми ігноруємо стандартну поведінку десктопних сайтів.

### Viewport Strategy
Критичні налаштування в `index.html`:
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover, interactive-widget=resizes-content">
```
*   `user-scalable=no`: Блокує зум пальцями.
*   `viewport-fit=cover`: Використовує простір під "чубчиком" (notch).

### Scroll Locking (The "App Feel")
Тіло сторінки (`body`) зафіксоване, скролиться тільки контент всередині контейнерів. Це прибирає ефект "гумової прокрутки" в Safari.

```css
body {
    position: fixed; 
    inset: 0; 
    overflow: hidden; /* Блокує скрол сторінки */
    overscroll-behavior: none;
}
.scrollable-content {
    overflow-y: auto; /* Скрол тільки тут */
    -webkit-overflow-scrolling: touch; /* Інерція на iOS */
}
```

### Safe Areas
Ми активно використовуємо CSS змінні середовища для iPhone X+:
*   `pt-safe`: `padding-top: env(safe-area-inset-top)`
*   `pb-safe`: `padding-bottom: env(safe-area-inset-bottom)`

---

## 5. ✨ Візуальні Ефекти (The Sauce)

### Noise Texture
На `body` накладено ледь помітний шар шуму (SVG filter).
*   **Мета:** Приховує "смугастість" (color banding) градієнтів на мобільних екранах низької якості.
*   **Реалізація:** `opacity: 0.02`, `pointer-events: none`.

### Scanlines & Grids
Фонові елементи використовують CSS-анімації для створення ефекту руху.
*   **Keyframes:** `gridFly` (3D обертання підлоги), `scanline` (вертикальний рух лінії).

### High Contrast Mode
У новій версії ми відмовилися від сірих кольорів на користь чистого білого (`text-white`) та напівпрозорого білого (`text-slate-400`), щоб забезпечити читабельність на сонці та максимальний контраст в темряві.

---

## 6. 🛡️ Статуси та Зворотній Зв'язок

### Haptics (Тактильний відгук)
Кожна дія супроводжується вібрацією.
*   **Click:** `haptic.impactOccurred('light')`
*   **Success:** `haptic.notificationOccurred('success')`
*   **Error:** `haptic.notificationOccurred('error')`

### Loaders
*   **Global:** Відсутній (Instant Boot).
*   **Local:** Skeleton-лоадери (`bg-white/5 animate-pulse`) всередині карток під час завантаження даних.

---

## 7. Приклад Реалізації (Card Component)

```tsx
const FuturisticCard = ({ children }) => (
  <div className="relative rounded-[1.5rem] overflow-hidden backdrop-blur-xl bg-brand-card border border-white/10 shadow-lg group transition-all duration-300 hover:border-brand-cyan/40">
    
    {/* Tech Corners (SVG-like styling via borders) */}
    <div className="absolute top-0 left-0 w-3 h-3 border-t-2 border-l-2 border-white/20 rounded-tl-lg group-hover:border-brand-cyan"></div>
    <div className="absolute top-0 right-0 w-3 h-3 border-t-2 border-r-2 border-white/20 rounded-tr-lg group-hover:border-brand-cyan"></div>
    
    {/* Content */}
    <div className="relative z-20 p-5">
      {children}
    </div>
  </div>
);
```

---

## 8. 🚀 Loading Screen Architecture (The "Neural Handshake")

Цей екран є візитною карткою додатку. Він має складну хореографію, яку необхідно відтворити точно.

### A. Механіка
1.  **Тривалість:** Рівно 6.5 секунд.
2.  **Потік:**
    *   0s - 2.5s: Сканування літер.
    *   2.5s - 6.0s: Передача енергії (частинки).
    *   6.0s - 6.5s: Фіналізація та перехід.

### B. Візуальні елементи
1.  **Літери (Vertical Stack):**
    *   Слово `STORKCRYPTO` відображається вертикально по центру.
    *   **3D Flip Effect:** Кожна літера — це "куб". Спочатку показується символ `?`, після проходження сканера він перевертається (`rotateX`) і показує справжню літеру.
    *   **Font:** `Orbitron`, Extra Bold (900), Neon Cyan (`#00F0FF`).

2.  **Частинки (Energy Transfer):**
    *   Використовується HTML5 Canvas.
    *   Частинки вилітають з кожної відкритої літери.
    *   **Ціль:** Летять по кривій до прогрес-бару.
    *   **Стиль:** Квадратні пікселі 2x2px, Cyan колір, Glowing shadow.

3.  **Кнопка пропуску (Skip Button):**
    *   **Розташування:** `absolute bottom-12`. Строго по центру горизонталі.
    *   **Текст:** `SKIP_INIT >>`.
    *   **Стиль:**
        *   Font: `Orbitron`.
        *   Size: `text-[10px]`.
        *   Color: `text-slate-600` (Default) -> `text-brand-cyan` (Hover).
        *   Tracking: `tracking-[0.3em]` (Дуже широкий трекінг).
    *   **Поведінка:** При натисканні миттєво перериває анімацію і відкриває Dashboard.

```tsx
// Приклад стилю кнопки SKIP
<button 
  className="absolute bottom-12 z-50 text-slate-600 hover:text-[#00d9ff] font-orbitron text-[10px] font-bold uppercase tracking-[0.3em] transition-colors duration-300 border-b border-transparent hover:border-[#00d9ff] pb-1 cursor-pointer"
>
  SKIP_INIT {'>>'}
</button>
```
