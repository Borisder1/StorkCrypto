import React from 'react';

// ⚠️ DEPRECATED: Voice commands removed for system optimization in Phase 3.
export const VoiceAssistant: React.FC<{ onCommand: (cmd: string) => void }> = () => {
    return null;
};